export default interface Options {
	deck: string,
	cardType: string,
	wordField: string,
	translationField: string,
	romajiField: string|null,
	hiraganaField: string|null,
	furiganaField: string|null,
}
